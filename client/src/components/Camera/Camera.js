import React from 'react';
import Camera from 'react-html5-camera-photo';
import 'react-html5-camera-photo-phot'

function Camera(props) {
  return (
    <h1>
      This is Used For Camera - changes done in local
    </h1>
  );
}

export default Camera;